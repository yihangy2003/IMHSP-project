server:
  port: 8081

spring:
  application:
    name: imhsp-user
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/imhsp?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root 